# Cache Horse benchmark

## Running benchmark

 1. Make sure Ruby is installed
 2. Install Ruby gems (`bundle install`)
 3. Run `cp .env-sample .env` to create an environment variable file
 4. Add your Cache Horse API key in `.env` to variable `CACHEHORSE_API_KEY`
 5. Run `rake benchmark:run` in your terminal to execute the benchmark
